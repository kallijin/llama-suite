# llama-suite Ideas Parking Lot

아이디어는 버리지 않는다.
하지만 모든 아이디어를 즉시 구현하지도 않는다.

## 규칙

- 떠오른 아이디어는 먼저 여기에 적는다.
- 바로 구현하지 않는다.
- core를 흔드는 아이디어는 최소 하루 묵힌다.
- 모듈 하나로 독립 가능한 아이디어만 우선 구현한다.
- 실행/삭제/시스템 경로/네트워크/대용량 탐색 관련 기능은 반드시 fuel limit과 수동 승인 원칙을 따른다.

## 방향 경고

- llama-suite는 Ollama나 LM Studio 같은 범용 런처의 자리를 바라보지 않는다.
- 이 도구는 그 아래층에서 llama.cpp, backend, 모델 파일, 실행 스크립트, probe, profile처럼 실무에 필요한 기반 지식을 다룬다.
- 초보자가 혼자 해결하기에는 어려운 부분을 파악하고, 접근 가능한 정비 도구로 낮춰 제공한다.
- 방향은 키다리 아저씨 같은 실무 지원, 착한 사마리아인 같은 공개 도구, 인간을 널리 이롭게 하는 홍익 정신이다.

## 분류

### Now
지금 구현해도 되는 작고 안전한 것.

- v0.4-scope-freeze 이후 기준에 맞춰 README와 IDEAS의 현재 상태, 구현됨/예정/보류 구분, public release 여부를 명확히 정리한다.
- 이미 구현된 메뉴와 모듈이 현재 로컬 환경에서 실행 가능한지 확인하고, 결과만 문서나 기록으로 남긴다.
- root 상태에서 제한 메뉴 접근 시 경고 메시지가 너무 빨리 지나가는 문제는 먼저 재현 여부를 확인한다. 재현 확인 전에는 바로 고치지 않는다.
- py_compile, git status, git diff처럼 코드 변경 없이 가능한 검증 명령을 수행한다. 일부는 `scripts/smoke-check.sh`로 실현했다.

### Next
곧 필요하지만 설계가 먼저 필요한 것.

- 핵심 우선순위: GGUF 모델이 실제 코딩/도구 사용에 적합한지 검증하고 보여주는 방식을 설계한다. direct tool-call, Hermes tool-call, chat template, reasoning 설정, 간단한 coding command intent 결과를 분리해서 표시하고, profiles 기록과 연결한다.
- Agent Readiness / Tools probe 판정 문구를 설계한다. 모델 목록 새로고침 시 모든 모델을 실제 로드해서 검사하지 않는다. 대신 profile에 저장된 cached status를 표시하고, 실제 tool-call probe는 사용자가 선택한 모델 또는 현재 실행 중인 llama.cpp 서버에 대해 수동으로 실행한다.
  - 상태 후보:
    - tools: 미확인 — 현재 실행 조합에서 tools 능력 발현 여부가 아직 확인되지 않은 상태
    - tools: 주의 — 현재 실행 조합에서 tools 능력이 불안정하게 발현될 수 있는 상태
    - tools: 실패 — 현재 실행 조합에서 tools 능력이 정상적으로 발현되지 않는 상태
    - tools: 부분 — direct tool-call은 확인됐지만 상위 에이전트 연동에서는 정상 발현이 확인되지 않은 상태
    - tools: 통과 — 현재 실행 조합에서 tools 능력 발현이 확인된 상태
  - 실패 문구 초안:

    ```text
    ❌ Tools probe 실패

    현재 llama.cpp 실행 조합에서 tools 능력이 정상적으로 발현되지 않는 상태입니다.

    이 결과는 원본 모델의 tools 능력이나 품질을 단정하지 않습니다.
    모델 공개 이후의 GGUF 변환, 양자화, 튜닝, chat template, 서버 옵션, tool-call parser, 상위 런처/에이전트 연동 방식 중 하나가 원인일 수 있습니다.

    이 상태의 모델은 에이전트 작업에 투입했을 때 런처가 로드를 거부하거나, 로드되더라도 실제 도구를 호출하지 않고 도구 실행처럼 보이는 텍스트만 생성할 수 있습니다.

    이 판정은 tools/tool-call 동작에 한정됩니다.
    일반적인 질의응답, 추론, 창작, 채팅 품질은 원본 모델 개발사가 공개한 공식 평가를 우선 신뢰합니다.
    ```

  - 원칙:
    - “모델이 tools를 지원하지 않는다”처럼 원본 모델을 단정하는 문구를 피한다.
    - “현재 llama.cpp 실행 조합에서 tools 능력이 발현되지 않는 상태”라고 표현한다.
    - 원본 모델, GGUF 변환, 양자화, 튜닝, chat template, parser, 상위 agent 연동을 분리해서 생각한다.
    - 모델 목록 새로고침은 가벼워야 하며 VRAM을 쓰는 실제 probe는 자동 실행하지 않는다.
    - tool_choice 방식별로 required, auto, named function choice를 분리해서 기록한다. direct tools PASS는 agent tools PASS가 아니며, 상위 agent wrapper 방식에 따라 결과가 달라질 수 있다.
- root 상태와 일반 유저 상태에서 실행 후 스크립트 관리 등 제한 메뉴로 접근했을 때 경고 메시지가 약 0.1초 만에 지나가 진입이 안 되는 버그처럼 보이는 문제를 재현했다. 수정 대기 상태로 두고, 다음 안정화 작업에서 경고 후 사용자가 읽을 수 있도록 확인 입력을 받거나 메뉴로 돌아가기 전 대기 처리를 검토한다.
- 런처가 떠 있는 동안 Hugging Face 등에서 새 모델을 다운로드한 뒤, 이를 반영할 마땅한 메뉴 흐름이 부족하다. 모델 목록 새로고침, 모델 디렉터리 재스캔, 새 모델 프로필 생성, 실행 스크립트 생성까지 이어지는 수동 메뉴 흐름을 고려한다.
- 모델 기본 지식 파일을 만들어 모델 선택/검색 시 초기 파라미터 후보를 제공하는 방안을 고려한다. 모델명, 버전, 계열, 출시 기준값, 일반 사용 파라미터, 코딩용 파라미터, 추론용 파라미터, chat template/tool-call/reasoning 힌트를 담고, 정확한 정보가 없으면 가까운 계열/버전의 값을 낮은 신뢰도 후보로 제시한다. 이 값은 로컬 모델이나 사용자의 판단을 돕는 정비 수첩이지 정답이 아니며, 자동 적용하지 않고 최종 선택은 사용자에게 맡긴다.

### Later
좋지만 지금 하면 프로젝트가 커지는 것.

- 언어장벽을 넘기 위한 유니코드화.

### Dangerous
재밌지만 시스템을 망가뜨릴 수 있는 것.

- `resolv.conf` 등 네트워크 설정을 참고해 endpoint 설정을 자동 추정하는 방안. 네트워크/시스템 경로 탐색이므로 fuel limit과 수동 승인 없이는 구현하지 않는다.
- 모델 디렉터리 검색 메뉴를 추가하는 방안. 기존 llama.cpp/backend discovery 계열 탐색 모듈과 연동하거나 별도 로직으로 만들 수 있지만, 대용량 경로 탐색이므로 수동 선택 root와 fuel limit 없이는 구현하지 않는다.
- 모든 세팅을 완료한 뒤 런처에서 OpenCode, Aider, Hermes Agent 같은 외부 에이전트에 파라미터를 넘기고 실행시키는 메뉴를 추가하는 방안. 외부 프로세스 실행 기능이므로 dry-run으로 실행 명령을 먼저 보여주고, 수동 승인 없이는 실행하지 않는다.

### Rejected
철학에 맞지 않아 버린 것.
